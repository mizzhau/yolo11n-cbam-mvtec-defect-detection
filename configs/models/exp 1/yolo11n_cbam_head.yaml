# YOLO11n + CBAM@Head Architecture Configuration
# CBAM integrated immediately before Detection heads at P3, P4, P5
# Dataset: MVTec AD 15 Categories / 48 Defect Types

nc: 48
scales:
  # [depth, width, max_channels]
  n: [0.50, 0.25, 1024]

# YOLO11n backbone (giữ nguyên — không có CBAM)
backbone:
  # [from, repeats, module, args]
  - [-1, 1, Conv, [64, 3, 2]]           # 0-P1/2
  - [-1, 1, Conv, [128, 3, 2]]          # 1-P2/4
  - [-1, 2, C3k2, [256, False, 0.25]]   # 2
  - [-1, 1, Conv, [256, 3, 2]]          # 3-P3/8
  - [-1, 2, C3k2, [512, False, 0.25]]   # 4
  - [-1, 1, Conv, [512, 3, 2]]          # 5-P4/16
  - [-1, 2, C3k2, [512, True]]          # 6
  - [-1, 1, Conv, [1024, 3, 2]]         # 7-P5/32
  - [-1, 2, C3k2, [1024, True]]         # 8
  - [-1, 1, SPPF, [1024, 5]]            # 9
  - [-1, 2, C2PSA, [1024]]              # 10

# YOLO11n head with CBAM just before Detect (ngay trước detection heads)
head:
  - [-1, 1, nn.Upsample, [None, 2, "nearest"]]  # 11
  - [[-1, 6], 1, Concat, [1]]                   # 12 cat backbone P4
  - [-1, 2, C3k2, [512, False]]                 # 13

  - [-1, 1, nn.Upsample, [None, 2, "nearest"]]  # 14
  - [[-1, 4], 1, Concat, [1]]                   # 15 cat backbone P3
  - [-1, 2, C3k2, [256, False]]                 # 16 (P3/8-small)
  - [-1, 1, CBAM, []]                           # 17-CBAM @ Head P3 (ngay trước Detect P3)

  - [-1, 1, Conv, [256, 3, 2]]                  # 18
  - [[-1, 13], 1, Concat, [1]]                  # 19 cat head P4
  - [-1, 2, C3k2, [512, False]]                 # 20 (P4/16-medium)
  - [-1, 1, CBAM, []]                           # 21-CBAM @ Head P4 (ngay trước Detect P4)

  - [-1, 1, Conv, [512, 3, 2]]                  # 22
  - [[-1, 10], 1, Concat, [1]]                  # 23 cat head P5
  - [-1, 2, C3k2, [1024, True]]                 # 24 (P5/32-large)
  - [-1, 1, CBAM, []]                           # 25-CBAM @ Head P5 (ngay trước Detect P5)

  - [[17, 21, 25], 1, Detect, [nc]]             # 26 Detect(P3, P4, P5)
